using UnityEngine;

public class TheCrazyScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Holy moly cube");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
